/* eslint-disable */
// @ts-nocheck

import "./home.styles.scss";
import Directory from "../../components/directory/directory.component";

const Home = () => {
  return (
    <section>
      <Directory />
    </section>
  );
};

export default Home;
